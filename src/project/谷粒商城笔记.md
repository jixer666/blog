# 谷粒商城笔记

<div style="background: #f8f9fa; padding: 12px 16px; border-left: 3px solid #4CAF50; margin-bottom: 16px; border-radius: 0 4px 4px 0; font-size: 0.9rem">
    <div style="display: flex; align-items: center; gap: 30px; flex-wrap: wrap;">
        <div style="display: flex; align-items: center; gap: 8px;">
            <span style="color: #666;">📅</span>
            <span>2025-07-26</span>
        </div>
        <div style="display: flex; align-items: center; gap: 8px;">
            <span style="color: #666;">✍️</span>
            <span>Jixer</span>
        </div>
    </div>
</div>

## ElasticSearch全文检索

ElastciSearch 入门笔记单独整理成一篇笔记：<a href="../java/ElasticSearch入门.html">点击跳转</a>

项目中在搜索商品的时候用到 ES 技术，笔者认为难点之处在于对 Java 操作 ES 的 API 语法的不熟悉

## Nginx 搭建

访问链路：Nginx -> 网关 -> 具体服务

### Host 配置

打开文件 `‪C:\Windows\System32\drivers\etc\hosts`，添加下行文字，保存并覆盖（需要用管理员权限）

```
127.0.0.1 gulimall.com
```

原理：浏览器在访问 `gulimall.com` 这个网站的时候，会先从本机中的 Host 文件去查找是否存在，否存在就返回对应设置的 IP 地址；若不存在就去解析  `gulimall.com` 去获取真实的 IP 地址

### Nginx 配置

作用：方向代理 + 负载均衡

注意：必须要添加 `proxy_set_header Host $host` ，否则在 `Nginx`  反向代理到网关是没有 `Host` 这个参数的，网关就匹配不到出现404错误

```nginx
http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;

    upstream gulimall {
        server 127.0.0.1:88;
    }

    server {
        listen       80;
        server_name  gulimall.com;

        location / {
            proxy_pass http://gulimall;
            proxy_set_header Host $host;
        }

        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }
    }

}

```

### 网关配置

```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: gulimall_host_route
          uri: lb://gulimall-product
          predicates:
            - Host=**.gulimall.com,gulimall.com # 根据 Host 进行断言
```

### 项目全链路流程

请求 -> nginx -> 网关 -> 服务

## 性能与压力测试

### 性能测试

**性能测试工具**

- jconsole
- jvisualvm

| 压测内容                    | 压测线程数 | 吞吐量/s | 90%响应时间 | 99%响应时间 |
| --------------------------- | ---------- | -------- | ----------- | ----------- |
| Nginx                       | 50         | 1580     | 33          | 35          |
| Gateway                     | 50         | 10980    | 6           | 35          |
| 简单服务                    | 50         | 15688    | 5           | 10          |
| 首页一级菜单渲染（DB+模板） | 50         | 465      | 182         | 296         |
| 三级分类数据获取（DB）      | 50         | 8        | 6866        | 8240        |
| 首页全量数据获取            | 50         | 84       | 2387        | 4171        |
| Nginx+Gateway               | 50         | 2201     | 29          | 48          |
| Gateway+简单服务            | 50         | 1860     | 53          | 164         |
| 全链路                      | 50         | 1809     | 58          | 172         |

优化后

| 压测内容                                                     | 压测线程数 | 吞吐量/s | 90%响应时间 | 99%响应时间 |
| ------------------------------------------------------------ | ---------- | -------- | ----------- | ----------- |
| 首页一级菜单渲染（加索引、该日志级别、加模板缓存）           | 50         | 1638     | 54          | 102         |
| 三级分类数据获取（加索引，改日志级别）                       | 50         | 16       | 4347        | 4968        |
| 首页全量数据获取（加索引、该日志级别、加模板缓存、动静分离） | 50         | 80       | 90          | 8316        |
| 三级分类数据获取（加索引，改日志级别、业务逻辑优化）         | 50         | 137      | 622         | 993         |
| 三级分类数据获取（加索引，改日志级别、业务逻辑优化、Redis缓存） | 50         | 603      | 210         | 384         |

由上表总结：

- 中间件越多，性能消耗越大，大多都损失在了网络交互上
- 适当的增加 Xms 、Xmx 可以提升吞吐量
- 对于一个接口，做性能与压力测试是必要的，很多时候请求量小并不能反映出问题。在测试的时候，需要结合上面两个测试工具一起对堆内存区进行分析，根据压测情况适当增加或减少堆内存大小。其次，业务逻辑对于读多写少的场景，能加缓存尽量加缓存。最后，SQL 语句也需要不断优化，根据数据量来考虑是否增加索引

### 压力测试

**常用的性能指标**

- HPS：每秒点击次数

- TPS：系统每秒处理交易数

- QPS：系统每秒处理查询次数

  对于互联网业务中，如果某些业务有且仅有一个请求连接，那么TPS = QPS = HPS，一 般情况下用 TPS 来衡量整个业务流程，用 QPS 来衡量接口查询次数，用 HPS 来表 示对服务器单击请求

- 吞吐量：每秒钟系统能够处理的请求数、任务数

- 响应时间：服务处理一个请求或一个任务的耗时

- 错误率：一批请求中结果出错的请求所占比例

**Jmeter 地址问题**

Jmeter 持续使用一段时间后会报错

```java
java.net.BindException: Address already in use: connect
```

解决办法：

- cmd 中用 `regedit` 命令打开注册表
- 在 `HKEY_LOCAL_MACHINE\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters` 下：
  - 右击 `parameters`，添加一个新的 `DWORD`，名字为 `MaxUserPort`
  - 然后双击 `MaxUserPort`，输入数值数据为65534，基数选择十进制

## 缓存

### 缓存穿透、缓存击穿和缓存雪崩

这块内容笔记单独整理成一篇笔记：<a href="黑马点评.html">点击跳转</a>

### Redis分布式锁实现

这块内容笔记单独整理成一篇笔记：<a href="../java/单机版Redis分布式锁.html">点击跳转</a>

### Redission分布式锁入门

#### 看门狗机制

作用：对使用 `lock` 方法的锁进行自动续期

Redission 的 `lock` 方法源代码中，若是没有传入值，就会创建一个定时任务，定时任务的默认时间是10s（看门狗的有效时间是30s，30 / 3 = 10），每10s会更新有效期。具体代码如下：

```java
private void renewExpiration() {
    ExpirationEntry ee = (ExpirationEntry)EXPIRATION_RENEWAL_MAP.get(this.getEntryName());
    if (ee != null) {
        Timeout task = this.commandExecutor.getConnectionManager().newTimeout(new TimerTask() {
            public void run(Timeout timeout) throws Exception {
                ExpirationEntry ent = (ExpirationEntry)RedissonLock.EXPIRATION_RENEWAL_MAP.get(RedissonLock.this.getEntryName());
                if (ent != null) {
                    Long threadId = ent.getFirstThreadId();
                    if (threadId != null) {
                        RFuture<Boolean> future = RedissonLock.this.renewExpirationAsync(threadId); // 设置有效期
                        future.onComplete((res, e) -> {
                            if (e != null) {
                                RedissonLock.log.error("Can't update lock " + RedissonLock.this.getName() + " expiration", e);
                            } else {
                                if (res) {
                                    RedissonLock.this.renewExpiration();
                                }
                            }
                        });
                    }
                }
            }
        }, this.internalLockLeaseTime / 3L, TimeUnit.MILLISECONDS); // internalLockLeaseTime默认是30s
        ee.setTimeout(task);
    }
}
```

#### 读写锁

读锁：

```java
ReadWriteLock lock = redissonClient.getReadWriteLock("test-wr-lock");
Lock readLock = lock.readLock();
```

写锁：

```java
ReadWriteLock lock = redissonClient.getReadWriteLock("test-wr-lock");
Lock writeLock = lock.writeLock();
```

结论：

- 读-读：共享
- 读-写：互斥
- 写-写：互斥
- 写-读：互斥

#### 信号量

获取一个信号量：

```java
RSemaphore semaphore = redissonClient.getSemaphore("test-semaphore");
semaphore.acquire(); // 若获取不到就陷入阻塞
// boolean b = semaphore.tryAcquire(); // 若获取不到就直接返回结果
```

释放一个信号量：

```java
RSemaphore semaphore = redissonClient.getSemaphore("test-semaphore");
semaphore.release();
```

使用场景：

- 分布式限流

#### 闭锁

类似 `CountDownLatch`，要等锁全部释放才能继续执行，可以用信号量实现

### 缓存一致性

#### 双写模式

在修改完数据库后，立刻取修改缓存，能够保证强一致性

缺点：高并发下容易出现脏数据，如下如所示：

![https://gitee.com/lijunxi666/picture-bed/raw/master/gulimall/Snipaste_2025-08-12_19-47-57.png](https://gitee.com/lijunxi666/picture-bed/raw/master/gulimall/Snipaste_2025-08-12_19-47-57.png)

#### 失效模式

- 方案一：修改完数据库后，删除缓存

  缺点：高并发下容易出现脏数据，如下图所示：

  ![https://gitee.com/lijunxi666/picture-bed/raw/master/gulimall/Snipaste_2025-08-12_19-52-44.png](https://gitee.com/lijunxi666/picture-bed/raw/master/gulimall/Snipaste_2025-08-12_19-52-44.png)

- 方案二：删除完缓存后，修改数据库

  缺点：容易出现脏数据

  线程 A 删完缓存后，线程 B 来查询缓存，此时不存在，就去查询数据库，加入缓存。然后线程 A 才更数据库，缓存中已存放了更新之前的值

**方案一解决方案**：

Cancal 订阅 MySQL binlog 日志

原理：数据库表更新字段信息后会写入 binlog 日志，用 Cancal 去监听 binlog，发送 MQ 来删除对应数据缓存

**方案二解决方案**

延时双删

再删除完缓存，修改完数据库后，延时一段时间（时间大于读数据 + 写缓存，一般为3~5秒），再去删除缓存。在删除缓存但未修改数据库期间，其他线程查询数据库向缓存中加入了旧数据，N 秒后会再次被删除

### SpringCache

#### 四种注解

SpringCache 有四种注解如下：

- @Cacheable：缓存方法返回的数据，key 这里设置的是方法名（也可以设置为方法的某一个参数，更具有灵活性），value 用于分组

  用法：`@Cacheable(value = "categoryCache", key="#root.method.name")`

- @CachePut：更新缓存为方法返回的数据

  用法：`@CachePut(value = "categoryCache", key="'getCache'")`

- @CacheEvict：删除缓存

  用法：`@CacheEvict(value = "categoryCache", key="'getCategoryListWithSpringCache'")`

- @Caching：组合操作

  用法：进行多个缓存删除

  ```java
  @Caching(evict = {
          @CacheEvict(value = "categoryCache", key = "'getCategoryListWithSpringCache'"),
          @CacheEvict(value = "categoryCache", key = "'getCache'")
  })
  // 上面的多次删除的注解等同于下面，删除同一种 value 的所有缓存
  @CacheEvict(value = "categoryCache", allEntries = true)
  ```

#### 自定义缓存配置

这里以 Redis 缓存为例，参考 `autoconfigure/cache/RedisCacheConfiguration` 的 `determineConfiguration` 方法的配置，自定义缓存的过期时间、缓存前缀和缓存序列化方式等

```java
@EnableCaching
@Configuration
@EnableConfigurationProperties(CacheProperties.class)
public class MyCacheConfig {
    @Bean
    public RedisCacheConfiguration redisCacheConfiguration(CacheProperties cacheProperties) {
        RedisCacheConfiguration config = RedisCacheConfiguration.defaultCacheConfig();
        config.serializeKeysWith(RedisSerializationContext.SerializationPair.fromSerializer(new StringRedisSerializer()));
        config.serializeValuesWith(RedisSerializationContext.SerializationPair.fromSerializer(new GenericJackson2JsonRedisSerializer()));
        CacheProperties.Redis redisProperties = cacheProperties.getRedis();
        if (redisProperties.getTimeToLive() != null) {
            config = config.entryTtl(redisProperties.getTimeToLive());
        }
        if (redisProperties.getKeyPrefix() != null) {
            config = config.prefixKeysWith(redisProperties.getKeyPrefix());
        }
        if (!redisProperties.isCacheNullValues()) {
            config = config.disableCachingNullValues();
        }
        if (!redisProperties.isUseKeyPrefix()) {
            config = config.disableKeyPrefix();
        }
        return config;
    }
}
```

#### 总结

SpringCache 适用于读多写少、一致性要求不高的场景

对于缓存三剑客问题：

- 缓存穿透：加配置 `spring.cache.redis.cache-null-values=true`解决

- 缓存击穿：单机模式上注解加 `@Cacheable(value = "categoryCache", key="#root.method.name", sync = true)` 可以解决，本质上使用 synchronized 来锁住获取方法，源码如下，但分布式下无法解决

  ```java
  public synchronized <T> T get(Object key, Callable<T> valueLoader) {
      Cache.ValueWrapper result = this.get(key);
      if (result != null) {
          return result.get();
      } else {
          T value = valueFromLoader(key, valueLoader);
          this.put(key, value);
          return value;
      }
  }
  ```

- 缓存雪崩问题：加随机的过期时间，还需要特殊化定制

## Thymeleaf

### 基础用法

- th:each：遍历元素
- th:attr：设置属性（比如新设置一个变量并赋值、自定义样式等）
- th:value：设置值（比如输入框）
- th:text：设置显示内容
- th:utext：设置显示内容（带有 html 格式）
- th:with：局部变量声明

项目具体使用见 gulimall-search 模块下的 list.html 文件

### 页面静态化过程

1、先在 `resources/tempaltes/list.html` 中配置页面模块

2、css、js 等静态文件放在 nginx 文件夹中，list.html 文件中的静态资源引用改为 nginx 下的相对路径

3、添加 host 文件域名配置

4、添加网关转发

5、配置 nginx 路由转发

请求一进来，参考 **Nginx 搭建中的项目全链路流程**

### Model和RedirectAttributes

Model 在重定向的时候会丢失数据，原因：请求302重定向后重新调用请求后数据会丢失

RedirectAttributes 在重定向的时候不会丢失数据，原因：RedirectAttributes 会将数据暂存到 session，然后请求302重定向后重新调用请求从 session 获取数据

具体代码见：`gulimall-auth` 模块下的 `LoginController` 文件里的 `register` 方法

## 线程

###  ThreadPoolExecutor 线程池

这块内容笔记单独整理成一篇笔记：<a href="../java/ThreadPoolExecutor源码解读.html">点击跳转</a>

### CompletableFuture

1、无返回值

```java
CompletableFuture.runAsync(() -> {
    System.out.println("无返回结果");
});
```

2、有返回值

```java
CompletableFuture<Integer> future = CompletableFuture.supplyAsync(() -> {
    System.out.println("有返回结果");
    return 10 / 2;
});
```

3、完成时回调

```java
CompletableFuture<Integer> future = CompletableFuture.supplyAsync(() -> {
    System.out.println("有返回结果");
    return 10 / 2;
}).whenComplete((result, exception) -> {
    try {
        Thread.sleep(2000);
        System.out.println("休眠结束");
    } catch (InterruptedException e) {
        throw new RuntimeException(e);
    }
});

// 异步处理
CompletableFuture<Integer> future = CompletableFuture.supplyAsync(() -> {
    System.out.println("有返回结果");
    return 10 / 2;
}).whenCompleteAsync((result, exception) -> {
    try {
        Thread.sleep(2000);
        System.out.println("休眠结束");
    } catch (InterruptedException e) {
        throw new RuntimeException(e);
    }
});
```

4、数据转换（类似于 stream 流的 map）

```java
CompletableFuture<String> future = CompletableFuture.supplyAsync(() -> 10 / 2)
    .thenApply(result -> {
        System.out.println("转换前: " + result); 
        return "结果是: " + result;
    });
```

5、 合并两个结果

```java
CompletableFuture<Integer> future1 = CompletableFuture.supplyAsync(() -> 10);
CompletableFuture<Integer> future2 = CompletableFuture.supplyAsync(() -> 20);

CompletableFuture<Integer> combined = future1.thenCombine(future2, (result1, result2) -> {
    System.out.println("结果1: " + result1);  // 输出: 结果1: 10
    System.out.println("结果2: " + result2);  // 输出: 结果2: 20
    return result1 + result2;
});

System.out.println(combined.get());  // 输出: 30
```

6、处理异常

```java
CompletableFuture<Integer> future = CompletableFuture.supplyAsync(() -> {
    System.out.println("有返回结果");
    return 10 / 2;
}).exceptionally(throwable -> {
    System.out.println("异常");
    return 10 / 3;
});
```

7、第3点和第6点结合，在完成后处理，也能处理异常，必须要有返回值

```java
CompletableFuture<Integer> future = CompletableFuture.supplyAsync(() -> {
    System.out.println("有返回结果");
    return 10 / 2;
}).handle((res, error) -> {
    System.out.println("异常：" + error.getMessage());
    return 10 / 3;
});
```

8、A、B 两个线程，A 执行完后在执行 B

```java
// 不需要A返回值
CompletableFuture.runAsync(() -> {
    System.out.println("任务一");
}).thenRun(() -> {
    System.out.println("任务二");
});

// 需要A返回值
CompletableFuture.supplyAsync(() -> {
    System.out.println("任务一");
    return 1;
}).thenAccept((res) -> {
    System.out.println("任务二，任务一返回值：" + res);
});

// 处理A返回值
CompletableFuture.supplyAsync(() -> {
    System.out.println("任务一");
    return 1;
}).thenApply((res) -> {
    System.out.println("任务二，任务一返回值：" + res);
    return "新结果，" + res;
});
```

9、A、B 两个线程，在两个线程同时执行完后执行 C

```java
// A、B有返回值
CompletableFuture<Integer> f1 = CompletableFuture.supplyAsync(() -> {
    System.out.println("任务一");
    return 1;
});
CompletableFuture<Integer> f2 = CompletableFuture.supplyAsync(() -> {
    System.out.println("任务二");
    return 2;
});
f1.thenAcceptBoth(f2, (r1, r2) -> {
    System.out.println("任务一结果：" + r1 + "，任务二结果：" + r2);
});

// A、B无返回值
CompletableFuture<Void> ff1 = CompletableFuture.runAsync(() -> {
    System.out.println("任务一");
});
CompletableFuture<Void> ff2 = CompletableFuture.runAsync(() -> {
    System.out.println("任务二");
});
ff1.runAfterBoth(ff2, () -> {
    System.out.println("任务一和任务二完成");
});
```

10、A、B 两个线程，在两个线程只要其中一个执行完后执行 C

```java
CompletableFuture<Integer> f1 = CompletableFuture.supplyAsync(() -> {
    System.out.println("任务一");
    return 1;
});
CompletableFuture<Integer> f2 = CompletableFuture.supplyAsync(() -> {
    try {
        Thread.sleep(1000);
        System.out.println("任务二");
    } catch (InterruptedException e) {
        throw new RuntimeException(e);
    }
    return 2;
});

// 无需返回值
f1.runAfterEither(f2, () -> {
    System.out.println("任务一或者任二其中一个处理完成");
});

// 有返回值
f1.acceptEither(f2, (res) -> {
    System.out.println("任务一或者任二其中一个处理完成，处理结果：" + res); // res输出1
});

// 处理为新结果
CompletableFuture<String> future = f1.applyToEither(f2, (res) -> {
    System.out.println("任务一或者任二其中一个处理完成，处理结果：" + res); // res输出1
    return "新结果" + res; // 输出 新结果1
});
```

12、A、B、C 三个线程同时执行完、或者只有一个执行才继续执行

```java
CompletableFuture<Integer> f1 = CompletableFuture.supplyAsync(() -> {
    System.out.println("任务一");
    return 1;
});
CompletableFuture<Integer> f2 = CompletableFuture.supplyAsync(() -> {
    try {
        Thread.sleep(1000);
        System.out.println("任务二");
    } catch (InterruptedException e) {
        throw new RuntimeException(e);
    }
    return 2;
});
CompletableFuture<Integer> f3 = CompletableFuture.supplyAsync(() -> {
    try {
        Thread.sleep(1000);
        System.out.println("任务三");
    } catch (InterruptedException e) {
        throw new RuntimeException(e);
    }
    return 3;
});
// 三个任务都执行完
CompletableFuture.allOf(f1, f2, f3).get();
// 三个任务其中一个执行完
CompletableFuture.anyOf(f1, f2, f3).get();
System.out.println("所有任务都执行完成");
```

**使用场景**

项目中在查询商品详情使用了异步编排，具体见 `gulimall-product` 模块下的 `ItemController` 代码文件

## Github 第三方登录

在 Github 中，新增一个自己的 OAuth2 APP，地址：[Authorized OAuth Apps](https://github.com/settings/applications)，获取到 client id 和 client secret

调用步骤

- 调用 `https://github.com/login/oauth/authorize?client_id=Ov23liZptJL6EpzSBc1g&redirect_uri=http://auth.gulimall.com/oauth2/github/callback&state=123Xdad&scope=userC3d12` GET请求，等用户授权后，会自动调用回调地址

- 调用 `https://github.com/login/oauth/access_token` POST 请求，传入以下格式的 json 数据获取 access_token
  ```java
  private Map<String, String> buildAccessTokenMap(String code) {
      Map<String, String> reqMap = new HashMap<>();
      reqMap.put("code", code);
      reqMap.put("redirect_uri", githubCallback);
      reqMap.put("client_id", githubClientId);
      reqMap.put("client_secret", githubClientSecret);
      return reqMap;
  }
  ```
  
- 调用 `https://api.github.com/user` GET 请求，请求头带上 `Authorization: token xxxxx`，来获取用户信息

- 注意：请求头均为 JSON 格式 `Accept: application/json`
